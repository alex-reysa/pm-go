/**
 * JSON Schema export for `SpecDecomposition`. Re-exports the
 * validator-side TypeBox schema (which is itself valid JSON Schema).
 */

export { SpecDecompositionSchema as SpecDecompositionJsonSchema } from "../../validators/orchestration-review/spec-decomposition.js";
