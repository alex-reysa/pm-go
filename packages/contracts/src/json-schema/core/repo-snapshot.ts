/**
 * JSON Schema export for `RepoSnapshot`. TypeBox schemas ARE JSON
 * Schema at runtime; re-exporting keeps validator and JSON Schema
 * definitions identical.
 */
export { RepoSnapshotSchema as RepoSnapshotJsonSchema } from "../../validators/core/repo-snapshot.js";
