/**
 * JSON Schema export for `AgentRun`. TypeBox schemas ARE JSON Schema
 * at runtime; re-exporting keeps validator and JSON Schema definitions
 * identical.
 */
export { AgentRunSchema as AgentRunJsonSchema } from "../../validators/core/agent-run.js";
