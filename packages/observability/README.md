# Observability

This package will define telemetry conventions and the durable event log model.

Primary responsibilities:

- OpenTelemetry trace and span naming
- structured execution events
- workflow and task correlation IDs
- operator-facing event summaries for UI streaming

