# Repo Intelligence

This package will gather repo context during spec intake.

Primary outputs:

- repo structure summary
- build and test command detection
- CI config discovery
- language and framework hints
- recent change summary
- coding convention hints

The output of this package becomes part of the durable repo snapshot contract.

